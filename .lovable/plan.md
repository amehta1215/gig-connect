Remove the black "Close" button from the footer of the read-only event preview dialog in `src/pages/artist/ArtistCalendar.tsx`. The dialog will continue to close via the built-in X button and clicking outside the overlay.